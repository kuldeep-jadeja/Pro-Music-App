---
phase: 01-admin-access-control
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - middleware.js
  - pages/admin/index.js
  - lib/AppContext.js
  - components/layout/Sidebar.js
  - pages/index.js
  - styles/Home.module.scss
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "Authenticated admin user can open `/admin` and nested `/admin/*` routes successfully."
    - "Unauthenticated user hitting any `/admin/*` route is redirected to `/login`."
    - "Authenticated non-admin user hitting any `/admin/*` route is redirected to `/` and sees 'Admin access required'."
    - "If admin rights are lost mid-session, next admin check/action redirects home after 403 without waiting past the next check cycle."
    - "Admin navigation entry points are visible only when `user.isAdmin` is true."
  artifacts:
    - path: "middleware.js"
      provides: "Wildcard route guard for all `/admin/:path*` page routes"
      contains: "matcher: ['/admin/:path*']"
    - path: "pages/admin/index.js"
      provides: "Admin landing page shell under protected wildcard route policy"
      exports: ["default"]
    - path: "lib/AppContext.js"
      provides: "5-minute admin recheck loop + immediate 403-first handling on admin surfaces"
      contains: "admin access recheck effect"
    - path: "components/layout/Sidebar.js"
      provides: "Admin-only navigation entry visibility"
      contains: "conditional admin link"
    - path: "pages/index.js"
      provides: "Post-redirect user-facing message for denied admin access"
      contains: "Admin access required"
  key_links:
    - from: "middleware.js"
      to: "pages/admin/*"
      via: "Next matcher /admin/:path*"
      pattern: "matcher:\s*\[\s*['\"]/admin/:path\*['\"]"
    - from: "lib/AppContext.js"
      to: "/api/admin/access-check"
      via: "runtime recheck + 403 redirect"
      pattern: "api/admin/access-check"
    - from: "components/layout/Sidebar.js"
      to: "user.isAdmin"
      via: "conditional link rendering"
      pattern: "user\?\.isAdmin"
---

<objective>
Implement wildcard admin-page access UX and continuous enforcement behavior aligned with locked session/redirect decisions.

Purpose: Satisfy ADMIN-01 and complete end-user behavior consistency for all `/admin/*` pages and mid-session authorization changes.
Output: Protected `/admin/*` route policy, admin-only nav entry, and redirect-message UX.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-access-control/1-CONTEXT.md
@.planning/phases/01-admin-access-control/01-RESEARCH.md
@.planning/phases/01-admin-access-control/01-01-PLAN.md
@lib/AppContext.js
@components/layout/Sidebar.js

<interfaces>
From lib/AppContext.js (current auth state contract):
```javascript
const [user, setUser] = useState(null);
const [authChecked, setAuthChecked] = useState(false);
const res = await fetch('/api/auth/me');
if (res.ok) {
  const data = await res.json();
  setUser(data.user);
}
```

From components/layout/Sidebar.js:
```javascript
export default function Sidebar({ user, playlists = [], onPlaylistClick, activePlaylistId, isOpen, onClose })
```

From plan 01 output contract:
```javascript
GET /api/admin/access-check -> 200 for admin, 403 for non-admin, 401 for unauthenticated
GET /api/auth/me -> { user: { id, email, isAdmin } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Enforce wildcard `/admin/*` route protection and keep admin landing page</name>
  <read_first>
    - .planning/phases/01-admin-access-control/1-CONTEXT.md
    - pages/_app.js
    - pages/login.js
  </read_first>
  <files>middleware.js, pages/admin/index.js</files>
  <behavior>
    - Test intent: unauthenticated request to `/admin` or `/admin/anything` redirects to `/login`.
    - Test intent: authenticated non-admin request to `/admin` or `/admin/anything` redirects to `/?adminAccess=required`.
    - Test intent: authenticated admin request to `/admin` is allowed through to render admin page content.
  </behavior>
  <action>
    Add root `middleware.js` with matcher `/admin/:path*` so *all* current and future admin page routes are protected by default (locked decision: protect all `/admin/*`). In middleware, evaluate session identity using existing auth/session primitives and server-derived admin status semantics from plan 01; unauthenticated users redirect to `/login`, authenticated non-admin users redirect to `/?adminAccess=required`, admins continue. Keep `pages/admin/index.js` as the phase-1 admin landing shell under this wildcard guard policy.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const m=fs.readFileSync('middleware.js','utf8');const p=fs.readFileSync('pages/admin/index.js','utf8');if(!/\/admin\/:path\*/.test(m)||!/adminAccess=required/.test(m)||!/login/.test(m)||p.length<50)process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - Wildcard matcher `/admin/:path*` is present and enforced in middleware.
    - Redirect targets match locked decision paths exactly (`/login` and `/`).
    - `pages/admin/index.js` remains the allowed admin landing route for authorized admins.
  </acceptance_criteria>
  <done>All `/admin/*` routes are protected by default, not just `/admin`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5-minute admin recheck and immediate 403-first handling</name>
  <read_first>
    - lib/AppContext.js
    - pages/api/auth/me.js
    - pages/api/admin/access-check.js
  </read_first>
  <files>lib/AppContext.js</files>
  <behavior>
    - Test intent: while on `/admin` (including nested paths), authorization recheck executes every 5 minutes.
    - Test intent: first 403 from admin access check triggers redirect to `/?adminAccess=required`.
    - Test intent: user state updates if `/api/auth/me` indicates admin flag changed.
  </behavior>
  <action>
    Extend `AppContext` to support admin-surface runtime enforcement. While the current route is `/admin` or starts with `/admin/`, run an interval check every 5 minutes against `/api/admin/access-check`. On any 403 response, immediately redirect to `/?adminAccess=required` and stop further admin actions for that session view. Keep server response as source of truth; do not rely on stale local role assumptions.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const c=fs.readFileSync('lib/AppContext.js','utf8');if(!/api\/admin\/access-check/.test(c)||!/(300000|5\s*\*\s*60\s*\*\s*1000)/.test(c)||!/adminAccess=required/.test(c)||!/\/admin/.test(c))process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - Recheck loop exists only for admin routes and uses 5-minute cadence.
    - API 403 is handled immediately without waiting for next interval.
    - Redirect path includes message flag consumed by home UI.
  </acceptance_criteria>
  <done>Mid-session admin-rights loss is enforced promptly and predictably.</done>
</task>

<task type="auto">
  <name>Task 3: Restrict admin navigation entry and surface denial message UX</name>
  <read_first>
    - components/layout/Sidebar.js
    - pages/index.js
    - styles/Home.module.scss
  </read_first>
  <files>components/layout/Sidebar.js, pages/index.js, styles/Home.module.scss</files>
  <action>
    Add an admin navigation link (e.g., `/admin`) rendered only when `user?.isAdmin` is true. Non-admin and unauthenticated users must not see admin entry points. In `pages/index.js`, read the `adminAccess=required` query flag and display a short, visible message exactly matching decision text: "Admin access required". Add minimal styling in `styles/Home.module.scss` for this message.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('components/layout/Sidebar.js','utf8');const h=fs.readFileSync('pages/index.js','utf8');const css=fs.readFileSync('styles/Home.module.scss','utf8');if(!/isAdmin/.test(s)||!/\/admin/.test(s)||!/Admin access required/.test(h)||css.length<20)process.exit(1);"</automated>
  </verify>
  <acceptance_criteria>
    - Admin link appears only for users with `isAdmin === true`.
    - Redirected non-admin users see "Admin access required" message on home.
    - Existing home page flows for playlist/import remain intact.
  </acceptance_criteria>
  <done>UI entry points and redirect feedback align with admin-only access policy.</done>
</task>

</tasks>

<verification>
Run each task-level automated smoke command after task completion, then run `npm test` once after full plan implementation.
</verification>

<success_criteria>
- Admin can open `/admin` and nested `/admin/*` routes; unauthenticated users are sent to `/login`.
- Signed-in non-admin users are redirected to home and shown "Admin access required" for any `/admin/*` attempt.
- Admin-only nav visibility and runtime 403 recheck behavior are active.
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-access-control/01-admin-access-control-02-SUMMARY.md`
</output>