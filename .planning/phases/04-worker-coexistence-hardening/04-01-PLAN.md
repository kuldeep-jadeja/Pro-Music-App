---
phase: 04-worker-coexistence-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/admin/workerCoexistenceContract.js
  - tests/worker-coexistence.smoke.js
  - scripts/workerCoexistenceMatrix.js
autonomous: true
requirements:
  - SYNC-02
must_haves:
  truths:
    - "Coexistence checks run in a repeatable before/during/after sequence instead of ad-hoc observation."
    - "Queue isolation evidence is automatically validated for demus:artist-expand:queue, demus:ytmatch:queue, and demus:metadata:queue."
    - "Core user/admin flows are probed during overlap and after overlap to surface regressions."
  artifacts:
    - path: "lib/admin/workerCoexistenceContract.js"
      provides: "Single source of truth for worker targets, queue keys, regression signals, and pass/fail thresholds"
    - path: "tests/worker-coexistence.smoke.js"
      provides: "Automated SYNC-02 smoke checks with --check queue-isolation|user-flows|overlap-regressions"
    - path: "scripts/workerCoexistenceMatrix.js"
      provides: "Repeatable coexistence scenario runner that emits machine-readable evidence"
  key_links:
    - from: "tests/worker-coexistence.smoke.js"
      to: "lib/admin/workerCoexistenceContract.js"
      via: "shared constants and threshold evaluation"
      pattern: "require\(.+workerCoexistenceContract"
    - from: "scripts/workerCoexistenceMatrix.js"
      to: "pages/api/import-playlist.js"
      via: "HTTP probe against /api/import-playlist during overlap"
      pattern: "/api/import-playlist"
    - from: "scripts/workerCoexistenceMatrix.js"
      to: "pages/api/admin/enqueue-artists.js"
      via: "HTTP probe against admin enqueue path"
      pattern: "/api/admin/enqueue-artists"
---

<objective>
Create the repeatable SYNC-02 validation harness (contract + smoke checks + coexistence matrix runner).

Purpose: Prove worker coexistence with measurable evidence while preserving Phase 2 queue-safe semantics.
Output: Contract constants, smoke checker CLI, and before/during/after matrix runner.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-worker-coexistence-hardening/04-CONTEXT.md
@.planning/phases/04-worker-coexistence-hardening/04-RESEARCH.md
@.planning/phases/04-worker-coexistence-hardening/04-VALIDATION.md
@workers/artistExpandWorker.js
@workers/ytMatchWorker.js
@workers/metadataWorker.js
@workers/artistCrawler.js
@workers/chartsWorker.js
@lib/artistExpandQueue.js
@lib/redisQueue.js
@lib/metadataQueue.js
@pages/api/import-playlist.js
@pages/api/playlists.js
@pages/api/playlist/[id]/status.js
@pages/api/admin/enqueue-artists.js
@pages/api/admin/retry-jobs.js

<interfaces>
From queue helpers:
```js
export const ARTIST_EXPAND_QUEUE_KEY = 'demus:artist-expand:queue';
export const QUEUE_KEY = 'demus:ytmatch:queue';
export const METADATA_QUEUE_KEY = 'demus:metadata:queue';
```

From workers:
```js
// workers/artistExpandWorker.js
const ARTIST_EXPAND_QUEUE_KEY = 'demus:artist-expand:queue';
const YTMATCH_QUEUE_KEY = 'demus:ytmatch:queue';
await redis.blpop(ARTIST_EXPAND_QUEUE_KEY, 30);
await redis.rpush(YTMATCH_QUEUE_KEY, JSON.stringify(job));

// workers/ytMatchWorker.js
const QUEUE_KEY = 'demus:ytmatch:queue';
await redis.blpop(QUEUE_KEY, 30);

// workers/metadataWorker.js
const QUEUE_KEY = 'demus:metadata:queue';
await redis.blpop(QUEUE_KEY, 5);
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define coexistence contracts and implement smoke check CLI</name>
  <read_first>
    - .planning/phases/04-worker-coexistence-hardening/04-CONTEXT.md
    - .planning/phases/04-worker-coexistence-hardening/04-RESEARCH.md
    - .planning/phases/04-worker-coexistence-hardening/04-VALIDATION.md
    - lib/artistExpandQueue.js
    - lib/redisQueue.js
    - lib/metadataQueue.js
    - pages/api/import-playlist.js
    - pages/api/playlists.js
    - pages/api/playlist/[id]/status.js
    - pages/api/admin/enqueue-artists.js
    - pages/api/admin/retry-jobs.js
  </read_first>
  <files>lib/admin/workerCoexistenceContract.js, tests/worker-coexistence.smoke.js</files>
  <behavior>
    - Test 1: `node tests/worker-coexistence.smoke.js --check queue-isolation` returns exit 0 only when queue-key and BLPOP isolation patterns match locked decisions.
    - Test 2: `node tests/worker-coexistence.smoke.js --check user-flows` returns exit 0 only when required flow probes (/api/import-playlist, /api/playlists, /api/playlist/[id]/status, /api/admin/enqueue-artists, /api/admin/retry-jobs) are covered.
    - Test 3: `node tests/worker-coexistence.smoke.js --check overlap-regressions --report <path>` fails when blocking regressions are present in matrix evidence JSON.
  </behavior>
  <action>Create `lib/admin/workerCoexistenceContract.js` exporting concrete constants for `WORKER_TARGETS` (`metadataWorker`, `artistCrawler`, `chartsWorker`, `ytMatchWorker`, `artistExpandWorker`), `QUEUE_KEYS`, `CORE_FLOW_ENDPOINTS`, and blocking regression codes required by context (`workers_not_consuming`, `playlist_flow_stalled`, `worker_crash_loop`, `queue_isolation_breach`). Then create `tests/worker-coexistence.smoke.js` as a Node CLI (no Jest) supporting `--check queue-isolation|user-flows|overlap-regressions` and optional `--report <jsonPath>`. Implement checks via deterministic source scanning + report validation; do not add new dependencies or bypass existing queue routes.</action>
  <acceptance_criteria>
    - `node -e "const c=require('./lib/admin/workerCoexistenceContract'); const w=['metadataWorker','artistCrawler','chartsWorker','ytMatchWorker','artistExpandWorker']; if(w.some(x=>!c.WORKER_TARGETS.includes(x))) process.exit(1);"`
    - `node tests/worker-coexistence.smoke.js --check queue-isolation`
    - `node tests/worker-coexistence.smoke.js --check user-flows`
  </acceptance_criteria>
  <verify>
    <automated>node tests/worker-coexistence.smoke.js --check queue-isolation && node tests/worker-coexistence.smoke.js --check user-flows</automated>
  </verify>
  <done>Contract file exists and smoke CLI enforces locked queue-isolation + core-flow coverage checks with deterministic pass/fail exits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build before/during/after coexistence matrix runner with evidence output</name>
  <read_first>
    - lib/admin/workerCoexistenceContract.js
    - tests/worker-coexistence.smoke.js
    - workers/artistExpandWorker.js
    - workers/ytMatchWorker.js
    - workers/metadataWorker.js
    - pages/api/import-playlist.js
    - pages/api/playlists.js
    - pages/api/playlist/[id]/status.js
    - pages/api/admin/enqueue-artists.js
    - pages/api/admin/retry-jobs.js
  </read_first>
  <files>scripts/workerCoexistenceMatrix.js</files>
  <behavior>
    - Test 1: `node scripts/workerCoexistenceMatrix.js --dry-run` exits 0 and prints planned baseline/overlap/recovery phases.
    - Test 2: with `--report <path>`, runner writes JSON evidence containing sections `baseline`, `overlap`, `recovery`, and `regressions`.
    - Test 3: overlap phase always includes admin enqueue/retry probes and user-flow probes in the same run.
  </behavior>
  <action>Create `scripts/workerCoexistenceMatrix.js` as a repeatable operator-run CLI using built-in Node modules (`fs`, `path`, `http/https`, `child_process` only). Required flags: `--base-url`, `--duration-seconds`, `--report`, `--dry-run`. Implement fixed three-stage sequence: baseline (user flows only), overlap (admin enqueue/retry + import/status polling + queue depth snapshots), recovery (post-expansion checks). Persist machine-readable evidence JSON for smoke `overlap-regressions` validation. Keep load profile realistic and sustained (batched probes, no synthetic stress loops) per user decision.</action>
  <acceptance_criteria>
    - `node scripts/workerCoexistenceMatrix.js --dry-run --base-url http://localhost:4072 --duration-seconds 120`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('scripts/workerCoexistenceMatrix.js','utf8');for (const k of ['/api/import-playlist','/api/playlists','/api/playlist/','/api/admin/enqueue-artists','/api/admin/retry-jobs']) if(!s.includes(k)) process.exit(1);"`
    - `node --check scripts/workerCoexistenceMatrix.js`
  </acceptance_criteria>
  <verify>
    <automated>node --check scripts/workerCoexistenceMatrix.js && node scripts/workerCoexistenceMatrix.js --dry-run --base-url http://localhost:4072 --duration-seconds 120</automated>
  </verify>
  <done>Matrix runner can be re-executed to generate before/during/after coexistence evidence and includes required overlap probes in one scenario.</done>
</task>
</tasks>

<verification>
- Run `node tests/worker-coexistence.smoke.js --check queue-isolation` and `--check user-flows` after each task commit.
- Run `node scripts/workerCoexistenceMatrix.js --dry-run --base-url http://localhost:4072 --duration-seconds 120` to confirm deterministic phase sequencing.
</verification>

<success_criteria>
- Coexistence harness artifacts required by 04-VALIDATION Wave 0 exist.
- Locked worker list, endpoint list, and regression signals are codified in executable checks.
- Evidence output is machine-readable for downstream verification/UAT.
</success_criteria>

<output>
After completion, create `.planning/phases/04-worker-coexistence-hardening/04-worker-coexistence-hardening-01-SUMMARY.md`
</output>


