---
phase: 04-worker-coexistence-hardening
plan: 02
type: execute
wave: 2
depends_on:
  - 04-01
files_modified:
  - workers/artistExpandWorker.js
  - scripts/workerCoexistenceReport.js
  - docs/phase-04-coexistence.md
  - package.json
autonomous: true
requirements:
  - SYNC-02
must_haves:
  truths:
    - "Artist expansion processing applies explicit yt-match backpressure handling so overlap does not silently starve core flows."
    - "Operators can run a single documented command path to produce and evaluate coexistence evidence."
    - "Phase 4 pass/fail is judged by blocking regression rules, not liveness-only logs."
  artifacts:
    - path: "workers/artistExpandWorker.js"
      provides: "Backpressure-aware enqueue behavior and structured coexistence diagnostics"
    - path: "scripts/workerCoexistenceReport.js"
      provides: "Deterministic pass/fail evaluator for matrix evidence JSON"
    - path: "docs/phase-04-coexistence.md"
      provides: "Operator runbook with before/during/after checklist and regression gates"
    - path: "package.json"
      provides: "Repeatable npm scripts for matrix + report + smoke verification"
  key_links:
    - from: "workers/artistExpandWorker.js"
      to: "demus:ytmatch:queue"
      via: "LLEN-based backpressure wait before RPUSH"
      pattern: "llen\(YTMATCH_QUEUE_KEY\)"
    - from: "scripts/workerCoexistenceReport.js"
      to: "scripts/workerCoexistenceMatrix.js"
      via: "reads matrix JSON evidence and computes regression verdict"
      pattern: "baseline|overlap|recovery"
    - from: "package.json"
      to: "tests/worker-coexistence.smoke.js"
      via: "coexistence:verify script chain"
      pattern: "coexistence:verify"
---

<objective>
Harden runtime coexistence behavior and package operator verification into a repeatable SYNC-02 pass/fail workflow.

Purpose: Prevent overlap regressions while giving verifier/UAT a concrete evidence path.
Output: Backpressure-aware worker behavior, report evaluator, runbook, and npm verification commands.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-worker-coexistence-hardening/04-CONTEXT.md
@.planning/phases/04-worker-coexistence-hardening/04-RESEARCH.md
@.planning/phases/04-worker-coexistence-hardening/04-VALIDATION.md
@.planning/phases/04-worker-coexistence-hardening/04-01-PLAN.md
@scripts/workerCoexistenceMatrix.js
@tests/worker-coexistence.smoke.js
@workers/artistExpandWorker.js
@package.json

<interfaces>
From worker code:
```js
const YTMATCH_QUEUE_KEY = 'demus:ytmatch:queue';
await redis.rpush(YTMATCH_QUEUE_KEY, JSON.stringify(job));
await redis.blpop(ARTIST_EXPAND_QUEUE_KEY, 30);
```

From matrix evidence contract:
```json
{
  "baseline": {},
  "overlap": {},
  "recovery": {},
  "regressions": []
}
```
</interfaces>
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Add yt-match backpressure guardrails to artistExpandWorker</name>
  <read_first>
    - workers/artistExpandWorker.js
    - workers/ytMatchWorker.js
    - .planning/phases/04-worker-coexistence-hardening/04-CONTEXT.md
    - .planning/phases/04-worker-coexistence-hardening/04-RESEARCH.md
    - scripts/workerCoexistenceMatrix.js
  </read_first>
  <files>workers/artistExpandWorker.js</files>
  <behavior>
    - Test 1: worker checks `LLEN demus:ytmatch:queue` before enqueue bursts and waits when depth exceeds configured threshold.
    - Test 2: if backpressure wait exceeds timeout, job is marked `failed` with a deterministic error code instead of silently pushing unbounded load.
    - Test 3: queue isolation remains unchanged (`BLPOP` only on artist-expand queue; yt-match queue outbound `RPUSH` only).
  </behavior>
  <action>In `workers/artistExpandWorker.js`, add explicit backpressure constants using env overrides (`ARTIST_EXPAND_YTMATCH_MAX_DEPTH`, `ARTIST_EXPAND_BACKPRESSURE_SLEEP_MS`, `ARTIST_EXPAND_BACKPRESSURE_MAX_WAIT_MS`) with safe defaults. Implement `waitForYtmatchCapacity(redis, context)` that polls `redis.llen(YTMATCH_QUEUE_KEY)` and blocks enqueue loop until depth is below threshold or timeout is exceeded. In `processJob`, call this helper before each outbound yt-match enqueue. On timeout, fail the ArtistJob with error code `ytmatch_backpressure_timeout` (preserve existing queue-safe semantics; do not add direct worker bypasses or cross-queue consumers). Keep existing `BLPOP(ARTIST_EXPAND_QUEUE_KEY, 30)` and outbound-only `RPUSH` topology unchanged.</action>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const s=fs.readFileSync('workers/artistExpandWorker.js','utf8');for(const k of ['ARTIST_EXPAND_YTMATCH_MAX_DEPTH','ARTIST_EXPAND_BACKPRESSURE_SLEEP_MS','ARTIST_EXPAND_BACKPRESSURE_MAX_WAIT_MS','llen(YTMATCH_QUEUE_KEY)','ytmatch_backpressure_timeout']) if(!s.includes(k)) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('workers/artistExpandWorker.js','utf8');if(!/blpop\(ARTIST_EXPAND_QUEUE_KEY,\s*30\)/.test(s)) process.exit(1); if(/blpop\(YTMATCH_QUEUE_KEY/.test(s)) process.exit(1);"`
    - `node --check workers/artistExpandWorker.js`
  </acceptance_criteria>
  <verify>
    <automated>node --check workers/artistExpandWorker.js && node tests/worker-coexistence.smoke.js --check queue-isolation</automated>
  </verify>
  <done>Artist expansion worker applies bounded backpressure-aware enqueue behavior without violating queue isolation contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add report evaluator, runbook, and packaged coexistence commands</name>
  <read_first>
    - scripts/workerCoexistenceMatrix.js
    - tests/worker-coexistence.smoke.js
    - .planning/phases/04-worker-coexistence-hardening/04-VALIDATION.md
    - package.json
  </read_first>
  <files>scripts/workerCoexistenceReport.js, docs/phase-04-coexistence.md, package.json</files>
  <behavior>
    - Test 1: report evaluator exits non-zero when evidence contains blocking regressions.
    - Test 2: report evaluator exits zero when evidence has no blocking regressions and all required sections are present.
    - Test 3: npm scripts expose `coexistence:matrix`, `coexistence:smoke`, and `coexistence:verify` for repeatable execution.
  </behavior>
  <action>Create `scripts/workerCoexistenceReport.js` that reads matrix JSON (`--report <path>`), validates required sections (`baseline`, `overlap`, `recovery`, `regressions`), and returns pass/fail based on locked blocking regression set from Plan 01 contract. Add `--sample pass|fail` mode so verification can run without live infrastructure. Create `docs/phase-04-coexistence.md` with explicit before/during/after run steps, required worker list, queue-key evidence checks, blocking regression definitions, and incident response note: diagnose-and-fix within Phase 4 scope. Update `package.json` scripts with exact entries: `coexistence:matrix`, `coexistence:smoke`, and `coexistence:verify` (chain matrix -> report -> smoke). Do not add telemetry dashboard features or autoscaling logic (deferred ideas).</action>
  <acceptance_criteria>
    - `node scripts/workerCoexistenceReport.js --sample pass`
    - `node scripts/workerCoexistenceReport.js --sample fail; if ($LASTEXITCODE -eq 0) { exit 1 }`
    - `node -e "const pkg=require('./package.json'); for (const k of ['coexistence:matrix','coexistence:smoke','coexistence:verify']) if(!pkg.scripts[k]) process.exit(1);"`
    - `node -e "const fs=require('fs');const d=fs.readFileSync('docs/phase-04-coexistence.md','utf8'); for(const t of ['metadataWorker','artistCrawler','chartsWorker','ytMatchWorker','artistExpandWorker','demus:artist-expand:queue','demus:ytmatch:queue','demus:metadata:queue']) if(!d.includes(t)) process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node scripts/workerCoexistenceReport.js --sample pass && node tests/worker-coexistence.smoke.js --check queue-isolation && node -e "const pkg=require('./package.json'); if(!pkg.scripts['coexistence:verify']) process.exit(1);"</automated>
  </verify>
  <done>Operators have a documented and scriptable coexistence verification workflow that enforces blocking regression policy.</done>
</task>
</tasks>

<verification>
- Run `npm run coexistence:smoke` after each commit in this plan.
- Run `node scripts/workerCoexistenceReport.js --sample pass` and `--sample fail` to validate pass/fail exits.
- Confirm `workers/artistExpandWorker.js` still isolates consumption to `demus:artist-expand:queue` and only pushes to `demus:ytmatch:queue`.
</verification>

<success_criteria>
- Backpressure handling exists for overlap hardening without cross-queue consumer shortcuts.
- Coexistence evidence can be generated and scored with deterministic blocking-regression gates.
- Runbook + npm scripts let verifier/UAT repeat the same scenario and compare outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/04-worker-coexistence-hardening/04-worker-coexistence-hardening-02-SUMMARY.md`
</output>
