---
phase: 03-admin-expansion-dashboard
plan: 03
type: execute
wave: 3
depends_on:
  - 03-02
files_modified:
  - pages/admin/index.js
  - styles/Admin.module.scss
autonomous: true
requirements:
  - QUEUE-01
  - SYNC-03
must_haves:
  truths:
    - "Admin can select multiple artists with row checkboxes and select-all-visible."
    - "Admin can bulk enqueue selected artists from /admin via existing queue-safe API."
    - "Admin sees per-item enqueue outcomes (queued/skipped/failed) with reason codes."
    - "Dashboard refreshes from server truth after bulk enqueue."
  artifacts:
    - path: "pages/admin/index.js"
      provides: "Selection model, bulk enqueue action, outcomes panel, post-action refresh"
    - path: "styles/Admin.module.scss"
      provides: "Selection + bulk-action + result-state styling"
  key_links:
    - from: "pages/admin/index.js"
      to: "/api/admin/enqueue-artists"
      via: "bulk queue button handler"
      pattern: "fetch\\('/api/admin/enqueue-artists"
    - from: "pages/admin/index.js"
      to: "/api/admin/artist-jobs"
      via: "refetch after bulk enqueue"
      pattern: "fetch\\('/api/admin/artist-jobs"
---

<objective>
Add bulk queue controls to complete end-to-end operator actions on /admin with queue-safe semantics and observable outcomes.

Purpose: Fulfill multi-select bulk enqueue requirement without introducing non-queue execution paths.
Output: Checkbox selection, select-all-visible, bulk enqueue action, and results rendering.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
@pages/admin/index.js
@styles/Admin.module.scss
@pages/api/admin/enqueue-artists.js
@pages/api/admin/artist-jobs.js

<interfaces>
From POST /api/admin/enqueue-artists:
```js
body: { artists: [{ spotifyId, name }] }
response: { summary: { total, queued, skipped, failed }, results: [{ artistSpotifyId, artistName, status, reason }] }
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement checkbox multi-select with select-all-visible reconciliation</name>
  <read_first>
    - pages/admin/index.js
    - .planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
    - .planning/phases/03-admin-expansion-dashboard/03-RESEARCH.md
  </read_first>
  <files>pages/admin/index.js, styles/Admin.module.scss</files>
  <action>Add selection state keyed by job `_id` plus mapping to `artistSpotifyId`/`artistName`. Add per-row checkbox and header checkbox for “select all visible” (current filtered dataset only). Reconcile selections after every jobs refresh/filter change by removing IDs not present in current visible rows to avoid hidden stale selections. Keep minimum 44x44 click targets for checkbox controls per UI spec.</action>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('select all visible')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('checkbox')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('setSelected')) process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!/selectAllVisible|allVisible/.test(s)) process.exit(1);"</automated>
  </verify>
  <done>Row and select-all-visible selection works on filtered rows, with stale/hidden selection cleanup on refresh.</done>
</task>

<task type="auto">
  <name>Task 2: Wire Queue Selected Artists bulk action and render per-item outcomes</name>
  <read_first>
    - pages/api/admin/enqueue-artists.js
    - pages/admin/index.js
    - .planning/phases/03-admin-expansion-dashboard/03-UI-SPEC.md
  </read_first>
  <files>pages/admin/index.js, styles/Admin.module.scss</files>
  <action>Add primary CTA text exactly `Queue Selected Artists`. Build payload from selected rows as `{ artists: [{ spotifyId: artistSpotifyId, name: artistName }] }` and call `POST /api/admin/enqueue-artists`. Disable CTA when no selected rows or while request is pending. Render returned `summary` counters and per-item results (queued/skipped/failed + reason) in a compact results panel. After success/failure, trigger immediate refetch from `/api/admin/artist-jobs` and clear selection only for rows whose outcome status is `queued`.</action>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('Queue Selected Artists')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('/api/admin/enqueue-artists')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');for(const t of ['queued','skipped','failed','summary']) if(!s.includes(t)) process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('/api/admin/artist-jobs')) process.exit(1);"</automated>
  </verify>
  <done>Bulk enqueue from selected artists works through existing API, shows reasoned outcomes, and immediately re-syncs dashboard state.</done>
</task>
</tasks>

<verification>
- Verify bulk action only uses `/api/admin/enqueue-artists` (no direct worker invocation).
- Verify outcome rendering includes reason codes for queued/skipped/failed.
- Verify immediate refresh occurs after enqueue to maintain SYNC-03 state trust.
</verification>

<success_criteria>
- Multi-select + select-all-visible are operational on /admin.
- Bulk enqueue action queues selected artists through existing queue-safe API.
- Per-item enqueue outcomes are visible with reason codes.
- Dashboard state updates immediately and via polling to reflect downstream status transitions.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-expansion-dashboard/03-admin-expansion-dashboard-03-SUMMARY.md`
</output>
