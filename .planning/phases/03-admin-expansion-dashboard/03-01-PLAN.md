---
phase: 03-admin-expansion-dashboard
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/admin/artistJobsContract.js
  - pages/api/admin/artist-jobs.js
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "Admin can retrieve artist expansion jobs across queued/running/done/failed from one API."
    - "Admin can apply status and text query filters together and get deterministic results."
    - "Failed-job rows include error and updated timestamp fields in API payload."
  artifacts:
    - path: "lib/admin/artistJobsContract.js"
      provides: "Shared filter/status constants and defaults for dashboard + API"
    - path: "pages/api/admin/artist-jobs.js"
      provides: "Admin-protected read endpoint with filter/search/sort/pagination"
  key_links:
    - from: "pages/api/admin/artist-jobs.js"
      to: "models/ArtistJob.js"
      via: "Mongoose query with status + regex filter"
      pattern: "ArtistJob.find"
    - from: "pages/api/admin/artist-jobs.js"
      to: "lib/requireAdmin.js"
      via: "HOF wrapping endpoint export"
      pattern: "export default requireAdmin\\(handler\\)"
---

<objective>
Create the server-truth read contract for the admin dashboard so UI work can safely consume job status, filtering, and failure metadata.

Purpose: Establish a stable API and shared constants before UI implementation.
Output: Shared contract constants + GET /api/admin/artist-jobs endpoint.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
@.planning/phases/03-admin-expansion-dashboard/03-RESEARCH.md
@models/ArtistJob.js
@pages/api/admin/enqueue-artists.js
@pages/api/admin/retry-jobs.js
@lib/requireAdmin.js

<interfaces>
From models/ArtistJob.js:
```js
status enum: ['queued', 'running', 'done', 'failed']
fields: artistSpotifyId, artistName, status, error, queuedAt, startedAt, completedAt, retriedAt, updatedAt
```

From existing admin write APIs:
```js
POST /api/admin/enqueue-artists -> { summary, results[] }
POST /api/admin/retry-jobs -> { summary, results[] }
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create shared dashboard filter/status contract constants</name>
  <read_first>
    - .planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
    - .planning/phases/03-admin-expansion-dashboard/03-RESEARCH.md
    - models/ArtistJob.js
  </read_first>
  <files>lib/admin/artistJobsContract.js</files>
  <action>Create `lib/admin/artistJobsContract.js` exporting exact constants: `JOB_STATUS_OPTIONS = ['all','queued','running','done','failed']`, `DEFAULT_STATUS_FILTER = 'all'`, `DEFAULT_QUERY = ''`, `DEFAULT_LIMIT = 50`, `MAX_LIMIT = 100`, and a pure helper `isValidJobStatusFilter(status)` that returns true only for listed values. Keep file dependency-free so both API route and page can import it.</action>
  <acceptance_criteria>
    - `node -e "const c=require('./lib/admin/artistJobsContract.js'); if(!c.JOB_STATUS_OPTIONS.includes('failed')) process.exit(1);"`
    - `node -e "const c=require('./lib/admin/artistJobsContract.js'); if(c.DEFAULT_LIMIT!==50||c.MAX_LIMIT!==100) process.exit(1);"`
    - `node -e "const c=require('./lib/admin/artistJobsContract.js'); if(c.isValidJobStatusFilter('bogus')) process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const c=require('./lib/admin/artistJobsContract.js'); if(!c.isValidJobStatusFilter('all')||!c.isValidJobStatusFilter('queued')) process.exit(1);"</automated>
  </verify>
  <done>Contract file exists with exported constants used as single source of truth for status/filter defaults.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement GET /api/admin/artist-jobs with combined filters and stable payload</name>
  <read_first>
    - pages/api/admin/enqueue-artists.js
    - pages/api/admin/retry-jobs.js
    - models/ArtistJob.js
    - lib/admin/artistJobsContract.js
    - lib/requireAdmin.js
  </read_first>
  <files>pages/api/admin/artist-jobs.js</files>
  <behavior>
    - Test 1: `status=failed&q=test` returns only failed jobs matching artist name or Spotify ID.
    - Test 2: Invalid status (e.g., `status=bad`) returns 400 with error payload.
    - Test 3: Response is sorted by `updatedAt` descending and includes error + updatedAt fields.
  </behavior>
  <action>Create admin-protected GET endpoint using `requireAdmin` + `connectDB`. Parse query params `status`, `q`, `page`, `limit` with defaults from `artistJobsContract`. Enforce `status` in allowed values; on invalid value return 400 `{ error: 'invalid_status_filter' }`. Escape regex special characters in `q` before Mongo filter. Build combined query: optional `status !== 'all'` AND optional `$or` on `artistName` + `artistSpotifyId` (case-insensitive contains). Return fields `_id, artistName, artistSpotifyId, status, error, updatedAt, queuedAt, startedAt, completedAt, retriedAt`. Sort `updatedAt: -1, _id: -1`. Implement pagination with `page>=1`, `limit<=100`. Return `{ items, pagination: { page, limit, total, totalPages }, filters: { status, q } }`.</action>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/api/admin/artist-jobs.js','utf8');if(!s.includes('export default requireAdmin(handler)'))process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/api/admin/artist-jobs.js','utf8');if(!/updatedAt:\\s*-1/.test(s))process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/api/admin/artist-jobs.js','utf8');if(!s.includes('invalid_status_filter'))process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('pages/api/admin/artist-jobs.js','utf8');for (const token of ['artistSpotifyId','artistName','status','error','pagination','filters']) if(!s.includes(token)) process.exit(1);"</automated>
  </verify>
  <done>Admin GET endpoint returns server-truth jobs with combinable filter/search behavior and failed-job metadata for dashboard rendering.</done>
</task>
</tasks>

<verification>
- Run endpoint contract checks from acceptance criteria.
- Confirm route only allows GET and rejects invalid status filters with 400.
</verification>

<success_criteria>
- `GET /api/admin/artist-jobs` exists and is `requireAdmin`-protected.
- Filter contract supports `all/queued/running/done/failed` and text query on artist name + Spotify ID.
- Payload includes failure reason and updated timestamp fields needed for VIS-03.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-expansion-dashboard/03-admin-expansion-dashboard-01-SUMMARY.md`
</output>
