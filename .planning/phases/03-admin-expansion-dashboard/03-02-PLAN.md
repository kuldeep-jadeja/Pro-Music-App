---
phase: 03-admin-expansion-dashboard
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - pages/admin/index.js
  - styles/Admin.module.scss
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
  - SYNC-03
must_haves:
  truths:
    - "Admin sees jobs by status in a dense table on /admin."
    - "Admin can combine status and text filters in UI."
    - "Failed rows show error reason and last updated value."
    - "Dashboard refreshes from server truth after retry action and on polling interval."
  artifacts:
    - path: "pages/admin/index.js"
      provides: "Jobs table, filters/search UI, retry action, polling-driven fetch"
    - path: "styles/Admin.module.scss"
      provides: "Table/filter/error/empty-state styles for admin dashboard"
  key_links:
    - from: "pages/admin/index.js"
      to: "/api/admin/artist-jobs"
      via: "fetchJobs() with current filter params"
      pattern: "fetch\\('/api/admin/artist-jobs"
    - from: "pages/admin/index.js"
      to: "/api/admin/retry-jobs"
      via: "row retry button action"
      pattern: "fetch\\('/api/admin/retry-jobs"
---

<objective>
Implement the dashboard read surface (filters + dense jobs table + retry + polling) using the Phase 3 API contract.

Purpose: Deliver trustworthy operational visibility and status synchronization behavior.
Output: Working /admin jobs dashboard (without bulk enqueue controls yet).
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
@.planning/phases/03-admin-expansion-dashboard/03-UI-SPEC.md
@pages/admin/index.js
@styles/Admin.module.scss
@pages/api/admin/artist-jobs.js
@pages/api/admin/retry-jobs.js
@lib/admin/artistJobsContract.js

<interfaces>
From GET /api/admin/artist-jobs:
```js
{
  items: [{ _id, artistName, artistSpotifyId, status, error, updatedAt, queuedAt, startedAt, completedAt, retriedAt }],
  pagination: { page, limit, total, totalPages },
  filters: { status, q }
}
```

From POST /api/admin/retry-jobs:
```js
body: { jobIds: string[] }
response: { summary, results[] } // reason includes retry_queued, already_active, job_not_found, invalid_job_id, redis_unavailable
```
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace /admin placeholder with filter bar + dense jobs table + empty/error states</name>
  <read_first>
    - pages/admin/index.js
    - styles/Admin.module.scss
    - .planning/phases/03-admin-expansion-dashboard/03-UI-SPEC.md
    - .planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
    - pages/api/admin/artist-jobs.js
  </read_first>
  <files>pages/admin/index.js, styles/Admin.module.scss</files>
  <action>In `pages/admin/index.js`, keep existing SSR `requireAdmin` guard intact. Replace placeholder body with two sections where this task implements the Jobs Table section first: status select (`all/queued/running/done/failed`, default all), query input matching artist name or Spotify ID, and dense table columns exactly: Status, Artist, Artist Spotify ID, Last Updated, Error, Actions. Fetch `/api/admin/artist-jobs` on mount and whenever status/query changes (250ms debounce for query). Render explicit empty-state copy from UI spec for both no-data and no-match scenarios. Add loading and load-error states with exact copy from UI spec. In SCSS, add compact table/filter styles honoring 4px spacing scale and existing admin theme variables.</action>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');for(const t of ['Status','Artist Spotify ID','Last Updated','No expansion jobs yet','No jobs match these filters']) if(!s.includes(t)) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('/api/admin/artist-jobs')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('styles/Admin.module.scss','utf8');for(const t of ['table','thead','tbody']) if(!s.includes(t)) process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('queued')||!s.includes('running')||!s.includes('done')||!s.includes('failed')) process.exit(1);"</automated>
  </verify>
  <done>/admin renders a dense, filterable jobs table with required columns and explicit empty/error states.</done>
</task>

<task type="auto">
  <name>Task 2: Add failed-row retry action with immediate refresh and 15s polling sync</name>
  <read_first>
    - pages/admin/index.js
    - pages/api/admin/retry-jobs.js
    - workers/artistExpandWorker.js
    - .planning/phases/03-admin-expansion-dashboard/03-CONTEXT.md
  </read_first>
  <files>pages/admin/index.js, styles/Admin.module.scss</files>
  <action>Implement row action button `Retry Failed Job` for rows where `status==='failed'`; disable for other statuses. On click, confirm with text `Retry Failed Job: Requeue this failed expansion job now?`, call `POST /api/admin/retry-jobs` with `{ jobIds: [row._id] }`, then always trigger immediate refetch of `/api/admin/artist-jobs`. Add polling loop at fixed 15000ms while page is mounted with proper cleanup; do not create duplicate intervals. Render per-row action feedback from retry response reason and visibly reflect status transition after refresh.</action>
  <acceptance_criteria>
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('/api/admin/retry-jobs')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('Retry Failed Job: Requeue this failed expansion job now?')) process.exit(1);"`
    - `node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!s.includes('15000')) process.exit(1);"`
  </acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs');const s=fs.readFileSync('pages/admin/index.js','utf8');if(!/setInterval\\(/.test(s)||!/clearInterval\\(/.test(s)) process.exit(1);"</automated>
  </verify>
  <done>Retry is operational for failed rows and dashboard state re-syncs immediately plus every 15s from server truth.</done>
</task>
</tasks>

<verification>
- Verify table/filter/retry UI code exists and references correct admin APIs.
- Verify polling interval has cleanup to prevent request leaks.
</verification>

<success_criteria>
- /admin shows queued/running/done/failed job states from API data.
- Filters (status + text query) are combinable in UI.
- Failed rows show `error` and `updatedAt`.
- Retry action updates dashboard state via immediate refresh + ongoing polling.
</success_criteria>

<output>
After completion, create `.planning/phases/03-admin-expansion-dashboard/03-admin-expansion-dashboard-02-SUMMARY.md`
</output>
