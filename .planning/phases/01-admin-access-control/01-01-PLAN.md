---
phase: 01-admin-access-control
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - lib/adminAccess.js
  - lib/requireAdmin.js
  - pages/api/auth/me.js
  - pages/api/admin/access-check.js
  - pages/api/admin/[...path].js
autonomous: true
requirements:
  - ADMIN-02
must_haves:
  truths:
    - "Authenticated non-admin users receive 403 for /api/admin/* routes."
    - "Configured admin email can call admin API and receive success."
    - "When ADMIN_EMAIL is missing, admin API access is denied with explicit server warning."
  artifacts:
    - path: "lib/adminAccess.js"
      provides: "Normalized admin email checks using ADMIN_EMAIL"
      contains: "normalizeEmail"
    - path: "lib/requireAdmin.js"
      provides: "Composable API guard that enforces admin-only access"
      exports: ["requireAdmin"]
    - path: "pages/api/admin/access-check.js"
      provides: "Admin-only probe endpoint for runtime authorization checks"
      exports: ["default"]
    - path: "pages/api/admin/[...path].js"
      provides: "Catch-all admin API fallback still enforcing admin guard"
      exports: ["default"]
  key_links:
    - from: "lib/requireAdmin.js"
      to: "lib/requireAuth.js"
      via: "wrapper composition"
      pattern: "requireAuth"
    - from: "pages/api/admin/access-check.js"
      to: "lib/requireAdmin.js"
      via: "route handler wrapping"
      pattern: "requireAdmin"
    - from: "pages/api/auth/me.js"
      to: "lib/adminAccess.js"
      via: "isAdmin computation for session payload"
      pattern: "isAdminEmail"
---

<objective>
Establish server-side admin authorization primitives and enforce forbidden responses across `/api/admin/*`.

Purpose: Satisfy ADMIN-02 and provide server-truth admin status used by page/session guards.
Output: Shared admin guard utilities plus protected admin API probe endpoints.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-access-control/1-CONTEXT.md
@.planning/phases/01-admin-access-control/01-RESEARCH.md
@lib/requireAuth.js
@pages/api/auth/me.js

<interfaces>
From lib/requireAuth.js:
```javascript
export function requireAuth(handler) {
  return async (req, res) => {
    const user = await getUserFromRequest(req);
    if (!user) return res.status(401).json({ error: 'Unauthorized' });
    req.user = user;
    return handler(req, res);
  };
}
```

From lib/auth.js:
```javascript
export async function getUserFromRequest(req) { /* resolves cookie JWT -> user */ }
```

From pages/api/auth/me.js (current response):
```javascript
return res.status(200).json({
  user: { id: user._id, email: user.email },
});
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create canonical admin identity + API guard utilities</name>
  <read_first>
    - .planning/phases/01-admin-access-control/1-CONTEXT.md
    - lib/requireAuth.js
    - lib/auth.js
  </read_first>
  <files>lib/adminAccess.js, lib/requireAdmin.js</files>
  <behavior>
    - Test intent: `normalizeEmail(" Admin@Site.com ")` yields `admin@site.com`.
    - Test intent: missing `ADMIN_EMAIL` causes admin checks to fail closed.
    - Test intent: authenticated non-admin through `requireAdmin` receives HTTP 403.
  </behavior>
  <action>
    Add `lib/adminAccess.js` with server-only helpers: normalized email comparison (`trim().toLowerCase()` on both configured and session email), `isAdminEmail(email)` boolean, and a missing-config warning path that logs once and denies access. Add `lib/requireAdmin.js` that composes `requireAuth` (do not duplicate auth parsing) and returns 403 JSON for authenticated non-admin users. Honor locked decision to use `ADMIN_EMAIL` as sole identity source and never expose it to client code.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `lib/adminAccess.js` exports normalized comparison helpers and fail-closed behavior when `ADMIN_EMAIL` is missing.
    - `lib/requireAdmin.js` wraps authenticated requests and emits 403 for non-admin identities.
    - No client bundle file imports `process.env.ADMIN_EMAIL` directly.
  </acceptance_criteria>
  <done>Admin authorization logic is centralized and reusable for all admin APIs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Enforce `/api/admin/*` guard coverage and expose admin status probe</name>
  <read_first>
    - lib/requireAdmin.js
    - pages/api/auth/me.js
    - .planning/phases/01-admin-access-control/01-RESEARCH.md
  </read_first>
  <files>pages/api/auth/me.js, pages/api/admin/access-check.js, pages/api/admin/[...path].js</files>
  <behavior>
    - Test intent: authenticated admin `GET /api/admin/access-check` returns 200.
    - Test intent: authenticated non-admin hitting `/api/admin/access-check` receives 403.
    - Test intent: unknown `/api/admin/*` path still requires admin and never leaks as public.
  </behavior>
  <action>
    Update `pages/api/auth/me.js` to include `isAdmin` in the returned user payload using `isAdminEmail(user.email)` so the frontend can render admin-only navigation without reading secrets. Create `pages/api/admin/access-check.js` (GET only) wrapped with `requireAdmin` and returning a lightweight success payload for runtime checks. Add `pages/api/admin/[...path].js` as guarded fallback returning 404 for unknown admin endpoints after admin authorization, ensuring prefix-wide protection behavior today and for future route additions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - `/api/auth/me` responds with `{ user: { id, email, isAdmin } }` when authenticated.
    - `/api/admin/access-check` enforces 401/403/200 behavior via wrappers, not inline duplication.
    - Catch-all admin API fallback exists and is also protected by `requireAdmin`.
  </acceptance_criteria>
  <done>All current admin API prefix behavior is server-enforced and provides an admin-status probe endpoint.</done>
</task>

</tasks>

<verification>
Run `npm test` after each task and once after the full plan. Confirm no unguarded `pages/api/admin/*` handler remains in this plan scope.
</verification>

<success_criteria>
- Non-admin authenticated requests to `/api/admin/access-check` receive HTTP 403.
- Authenticated admin requests to `/api/admin/access-check` receive HTTP 200.
- `/api/auth/me` includes `isAdmin` derived only from server-side `ADMIN_EMAIL` logic.
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-access-control/01-admin-access-control-01-SUMMARY.md`
</output>
