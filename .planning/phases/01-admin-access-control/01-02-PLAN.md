---
phase: 01-admin-access-control
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - pages/admin/index.js
  - lib/AppContext.js
  - components/layout/Sidebar.js
  - pages/index.js
  - styles/Home.module.scss
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
must_haves:
  truths:
    - "Authenticated admin user can open `/admin` successfully."
    - "Unauthenticated user hitting `/admin` is redirected to `/login`."
    - "Authenticated non-admin user is redirected to `/` and sees 'Admin access required'."
    - "If admin rights are lost mid-session, next admin check/action redirects home after 403 without waiting past the next check cycle."
    - "Admin navigation entry points are visible only when `user.isAdmin` is true."
  artifacts:
    - path: "pages/admin/index.js"
      provides: "Protected admin page shell and route-level redirect behavior"
      exports: ["default", "getServerSideProps"]
    - path: "lib/AppContext.js"
      provides: "5-minute admin recheck loop + immediate 403-first handling on admin surfaces"
      contains: "admin access recheck effect"
    - path: "components/layout/Sidebar.js"
      provides: "Admin-only navigation entry visibility"
      contains: "conditional admin link"
    - path: "pages/index.js"
      provides: "Post-redirect user-facing message for denied admin access"
      contains: "Admin access required"
  key_links:
    - from: "pages/admin/index.js"
      to: "/api/admin/access-check"
      via: "runtime admin status validation"
      pattern: "fetch\('/api/admin/access-check'"
    - from: "lib/AppContext.js"
      to: "next/router"
      via: "403-triggered redirect to home"
      pattern: "router.push\('/\?adminAccess=required'\)"
    - from: "components/layout/Sidebar.js"
      to: "user.isAdmin"
      via: "conditional link rendering"
      pattern: "user\?\.isAdmin"
---

<objective>
Implement admin-only page access UX and continuous enforcement behavior aligned with locked session/redirect decisions.

Purpose: Satisfy ADMIN-01 and complete end-user behavior consistency for admin pages and mid-session authorization changes.
Output: Protected `/admin` page, admin-only nav entry, and redirect-message UX.
</objective>

<execution_context>
@~/.copilot/get-shit-done/workflows/execute-plan.md
@~/.copilot/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-admin-access-control/1-CONTEXT.md
@.planning/phases/01-admin-access-control/01-RESEARCH.md
@.planning/phases/01-admin-access-control/01-01-PLAN.md
@lib/AppContext.js
@components/layout/Sidebar.js

<interfaces>
From lib/AppContext.js (current auth state contract):
```javascript
const [user, setUser] = useState(null);
const [authChecked, setAuthChecked] = useState(false);
const res = await fetch('/api/auth/me');
if (res.ok) {
  const data = await res.json();
  setUser(data.user);
}
```

From components/layout/Sidebar.js:
```javascript
export default function Sidebar({ user, playlists = [], onPlaylistClick, activePlaylistId, isOpen, onClose })
```

From plan 01 output contract:
```javascript
GET /api/admin/access-check -> 200 for admin, 403 for non-admin, 401 for unauthenticated
GET /api/auth/me -> { user: { id, email, isAdmin } }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add protected `/admin` page with server redirect policy</name>
  <read_first>
    - .planning/phases/01-admin-access-control/1-CONTEXT.md
    - lib/auth.js
    - pages/login.js
  </read_first>
  <files>pages/admin/index.js</files>
  <behavior>
    - Test intent: unauthenticated request to `/admin` redirects to `/login`.
    - Test intent: authenticated non-admin request to `/admin` redirects to `/?adminAccess=required`.
    - Test intent: authenticated admin request renders admin page content.
  </behavior>
  <action>
    Create `pages/admin/index.js` with `getServerSideProps` that resolves the user from request and evaluates admin status via shared admin utility. Enforce locked decisions exactly: unauthenticated -> `/login`, authenticated non-admin -> home with message flag, admin -> render. Keep this page as minimal admin shell for Phase 1 (no queue/dashboard feature depth yet).
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Route `/admin` exists and performs server-side redirects without client-only race conditions.
    - Redirect targets match locked decision paths exactly (`/login` and `/`).
    - Admin request reaches rendered page output.
  </acceptance_criteria>
  <done>Admin page access is correctly gated by server-side identity checks.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add 5-minute admin recheck and immediate 403-first handling</name>
  <read_first>
    - lib/AppContext.js
    - pages/api/auth/me.js
    - pages/api/admin/access-check.js
  </read_first>
  <files>lib/AppContext.js</files>
  <behavior>
    - Test intent: while on `/admin`, authorization recheck executes every 5 minutes.
    - Test intent: first 403 from admin access check triggers redirect to `/?adminAccess=required`.
    - Test intent: user state updates if `/api/auth/me` indicates admin flag changed.
  </behavior>
  <action>
    Extend `AppContext` to support admin-surface runtime enforcement. While the current route is `/admin` (or starts with `/admin/`), run an interval check every 5 minutes against `/api/admin/access-check`. On any 403 response, immediately redirect to `/?adminAccess=required` and stop further admin actions for that session view. Keep server response as source of truth; do not rely on stale local role assumptions.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Recheck loop exists only for admin routes and uses 5-minute cadence.
    - API 403 is handled immediately without waiting for next interval.
    - Redirect path includes message flag consumed by home UI.
  </acceptance_criteria>
  <done>Mid-session admin-rights loss is enforced promptly and predictably.</done>
</task>

<task type="auto">
  <name>Task 3: Restrict admin navigation entry and surface denial message UX</name>
  <read_first>
    - components/layout/Sidebar.js
    - pages/index.js
    - styles/Home.module.scss
  </read_first>
  <files>components/layout/Sidebar.js, pages/index.js, styles/Home.module.scss</files>
  <action>
    Add an admin navigation link (e.g., `/admin`) rendered only when `user?.isAdmin` is true. Non-admin and unauthenticated users must not see admin entry points. In `pages/index.js`, read the `adminAccess=required` query flag and display a short, visible message exactly matching decision text: "Admin access required". Add minimal styling in `styles/Home.module.scss` for this message.
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <acceptance_criteria>
    - Admin link appears only for users with `isAdmin === true`.
    - Redirected non-admin users see "Admin access required" message on home.
    - Existing home page flows for playlist/import remain intact.
  </acceptance_criteria>
  <done>UI entry points and redirect feedback align with admin-only access policy.</done>
</task>

</tasks>

<verification>
Run `npm test` after each task and once after full plan. Confirm `/admin` redirect behavior and admin-link visibility in dev verification notes for summary.
</verification>

<success_criteria>
- Admin can open `/admin`; unauthenticated users are sent to `/login`.
- Signed-in non-admin users are redirected to home and shown "Admin access required".
- Admin-only nav visibility and runtime 403 recheck behavior are active.
</success_criteria>

<output>
After completion, create `.planning/phases/01-admin-access-control/01-admin-access-control-02-SUMMARY.md`
</output>
